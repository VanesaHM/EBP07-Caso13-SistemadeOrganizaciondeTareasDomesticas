# En Render, SoylaApplication normaliza DATABASE_URL (postgresql://...)
# a una JDBC URL valida antes de que Spring inicialice el DataSource.
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://${DATABASE_HOST:localhost}:${DATABASE_PORT:5432}/${DATABASE_NAME:soyla}}
